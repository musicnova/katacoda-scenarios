You can display the current configuration by issuing `config show`{{execute}}

If you want to validate the configuration before running the provisioning of the cluster, use `config validate`{{execute}}